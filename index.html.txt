<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Website đầu tiên của tôi</title>

  <style>
    * { box-sizing: border-box; }

    body {
      margin: 0;
      font-family: Arial, sans-serif;
      background: linear-gradient(135deg, #f6f9ff, #eef7ff);
      color: #1f2d3d;
      padding: 24px;
    }

    .container {
      max-width: 900px;
      margin: 0 auto;
    }

    .card {
      background: white;
      border-radius: 16px;
      padding: 24px;
      box-shadow: 0 10px 30px rgba(0,0,0,0.08);
      display: grid;
      grid-template-columns: 240px 1fr;
      gap: 24px;
      align-items: center;
    }

    .avatar {
      width: 100%;
      max-width: 240px;
      aspect-ratio: 1 / 1;
      object-fit: cover;
      border-radius: 999px;
      border: 6px solid #e8f2ff;
    }

    h1 {
      margin: 0 0 8px;
      font-size: 32px;
    }

    p {
      margin: 0 0 16px;
      line-height: 1.6;
      color: #334e68;
    }

    .buttons {
      display: flex;
      gap: 12px;
      flex-wrap: wrap;
    }

    button, a.btn {
      padding: 12px 16px;
      border-radius: 10px;
      border: none;
      cursor: pointer;
      font-size: 16px;
      text-decoration: none;
      display: inline-block;
    }

    .primary {
      background: #2f80ed;
      color: white;
    }

    .secondary {
      background: #e8f2ff;
      color: #2f80ed;
    }

    .primary:hover { opacity: 0.92; }
    .secondary:hover { filter: brightness(0.98); }

    .footer {
      text-align: center;
      margin-top: 18px;
      font-size: 14px;
      color: #6b7c93;
    }

    /* 3) 📱 Responsive: khi màn hình nhỏ (điện thoại), chuyển sang 1 cột */
    @media (max-width: 700px) {
      body { padding: 14px; }
      .card {
        grid-template-columns: 1fr;
        text-align: center;
      }
      .avatar { margin: 0 auto; }
      .buttons { justify-content: center; }
      h1 { font-size: 26px; }
    }
  </style>
</head>

<body>
  <div class="container">
    <div class="card">
      <img class="avatar" src="images/avatar.jpg" alt="Ảnh của tôi" />

      <div>
        <h1>Xin chào! 👋</h1>
        <p>
          Mình đang học làm web từ con số 0. Đây là trang demo đầu tiên:
          có ảnh, giao diện đẹp hơn và hiển thị tốt trên điện thoại.
        </p>

        <div class="buttons">
          <button class="primary" onclick="alert('Bạn đã bấm nút!')">
            Bấm vào đây
          </button>

          <a class="btn secondary" href="https://google.com" target="_blank">
            Mở Google
          </a>
        </div>
      </div>
    </div>

    <div class="footer">
      © 2025 - Website đầu tiên của tôi
    </div>
  </div>
</body>
</html>
